# 扫频 + 相干采样模块 — 进度报告 & 待办

随手记的进度,方便接着干。日期:2026-06-30。

---

## 一、现在做到哪了

整条链路是:**AD9833/信号源 → DUT → 双 ADC 同步采样 → Goertzel 单点 → 算 H(f) → (校准) → 出表**。
分模块板上调过来,主要的坑都填了,核心能跑到 1MHz。

### 已经搞定的(STAGE 0~3 全过 + 校准已实现)

- **时钟**:绕了个弯——之前在 CubeMX 改 ADC 时钟把主频从 480MHz 拽到了 75MHz,后来恢复了。现在:
  - SYSCLK 480MHz,TIM4 定时器内核 **240MHz**,ADC 内核 75MHz。
- **AD9833 出波形**:一开始死活不出/电平乱跳,查到根因其实是 **`dds_set_frequency` 没在头文件里声明**,float 参数被当 double 传(硬浮点 ABI 错位),频率成了垃圾值。补上原型就好了。驱动本身用回了 afterxiaosai 验证过的版本。
- **ADC 采样**:
  - 一开始死等卡死,原因是 ADC1 被配成双重模式(REGSIMULT)但代码用两个独立 `Start_DMA`。最后改成**独立双 ADC + 共享 TIM4 触发**(两路同一个触发边沿,天然同步采样),one-shot + DMA Normal,12-bit。
  - 实测 ADC 均匀采样上限 ~**1.2MHz**(再快就跳触发、采样不均匀,会破坏相干)。
- **相干欠采样(重点)**:这是能测到 1MHz 的关键。
  - 公式:DDS 出 `f_actual = (p + 1/N)·Fs`,高频用低采样率让信号**混叠**到 bin=M。
  - 两路同步采样、同样混叠,所以 `|H|` 和 `∠H`(相位差)完全不受混叠影响。
  - 实测:**1MHz 用 800kHz 采样(p=1),|H|=1.00,相位≈0**;100kHz 过采样也 OK。✅
- **直通校准(thru-cal)做完并验证**:
  - 两路有个**固定采样偏斜**,相位误差随频率线性涨。实测 1MHz≈-0.67°(对应 τ≈**1.86ns**),100kHz≈-0.09°。
  - 注意:这个偏斜**不同上电有 ~0.3° 漂移**(早先测过 -0.96°)。
  - **当前用硬编码**:`calib.c` 的 `cal_apply_correction` 直接用 `CAL_SKEW_S=1.86ns` 减掉偏斜(+ 增益 /1.0008),不用每次跑校准。符号已用 STAGE3 同源验证:1MHz/100kHz 校准后都拉回 ~0°。
  - **运行时 thru-cal 也保留**(`cal_run_thru` 扫表 + 线性插值),要更高相位精度(<0.1°、抓当前漂移)时切过去用。

### 关键数字速查

| 项 | 值 |
|----|----|
| TIM4 内核 | 240MHz (`TIM_KER_CLK_HZ`) |
| ADC 均匀采样上限 | 1.2MHz (`FS_MAX_HZ`) |
| ADC 分辨率 | 12-bit |
| 过/欠采样分界 | f ≈ 300kHz(以下过采样 p=0,以上欠采样 p≥1) |
| 通道间偏斜 | ~1.86ns(硬编码校准, `CAL_SKEW_S`;有 ~0.3° 上电漂移) |
| 校准后相位 | 1MHz/100kHz 同源实测均 ~0°(已验证) |

---

## 二、怎么用这套调试框架

- 总开关在 `Core/Inc/config.h`:`DEBUG_SWEEP`(注释掉就回正常运行模式,跑 `UART_Poll`)。
- 选模块:改 `config.h` 里 `DEBUG_STAGE`,重新编译烧录。串口 115200。

| STAGE | 干啥 | 接线 |
|-------|------|------|
| 0 | 时钟自检 | 无 |
| 1 | AD9833 设频(示波器看) | AD9833 出 |
| 2 | 采样率实测(DWT) | 信号源进 ADC |
| 3 | 单点相干验证(外部源) | 信号源并联 CH1+CH2,频率设 `STAGE3_FGEN` |
| **4** | **直通校准验证** | AD9833 接 CH1+CH2,看校准前后相位 |
| **5** | **校准 + 全段扫频画 Bode** | 先 AD9833 接 CH1+CH2(校准),再接 DUT |

---

## 三、接下来要做的(待办)

### 马上要做
- [x] **校准符号已验证**:STAGE3 同源,1MHz/100kHz 校准后相位都 ~0°。
- [ ] **跑 STAGE5 扫真实滤波器**:接个已知 RC 低通(如 R=1.6k C=1nF, fc≈100kHz),看 Bode 斜率(-20dB/dec)、-3dB 点对不对。这是整条链的最终验收。
- [ ] **确认 ADC 前端带宽 > 1MHz**:欠采样的前提,如果前面有截止低于 1MHz 的抗混叠滤波,高频信号会被吃掉。

### 后面优化(不急)
- [ ] **扫频网格细化**:`sweep_grid.c` 现在只有粗扫(对数等比),自适应细化(斜率大的地方加密)还是个桩,规范 §7.2。
- [ ] **校准数据存 Flash**:现在 `g_thru` 在 RAM,掉电就没了,每次开机要重校。可以存一份到 Flash。
- [ ] **UART 命令触发**:现在靠改 `DEBUG_STAGE` 重编译切换,正式用应该用串口命令触发"校准/扫频/读结果"。
- [ ] **判稳参数调优**:`sweep_engine` 里 `max_timeout=20`、`SETTLE_TH=0.5%` 这些值后面按实际信号调。
- [ ] **AD9833 自动量程**:`hooks.c` 的 `gen_set_drive_level` 还是空桩,信号太小/太大时自动调幅度。
- [ ] **接 Nextion 屏**:关掉 DEBUG_SWEEP,把结果显示到屏上。

### 已知小坑(记着别再踩)
- 跨文件函数(尤其 float 参数的)**一定要在头文件声明**,否则硬浮点 ABI 会静默传错值。已加 `-Werror=implicit-function-declaration` 兜底。
- D-Cache 没开,DMA 缓冲别调 `SCB_*`,会 HardFault。
- ADC 的 Conversion Data Management 必须和 DMA 流的 Mode 一致(都 one-shot/Normal 或都 circular)。

---

## 四、文件清单

| 文件 | 干啥 |
|------|------|
| `Core/Src/sweep_engine.c` | 单点测量(含相干欠采样参数计算)、扫频主控 |
| `Core/Src/goertzel.c` | 单 bin Goertzel,算两路复数比 H |
| `Core/Src/adc_sync.c` | 采集窗口封装(包到 `ADC_SampleOnce_TIM4`) |
| `Core/Src/calib.c` | 直通校准(新实现) |
| `Core/Src/sweep_grid.c` | 扫频网格(粗扫,细化待补) |
| `Core/Src/hooks.c` | DDS 设频等外部接口(接 AD9833) |
| `Core/Inc/config.h` | 所有常量 + 调试开关 |

---

## 五、进展更新 (2026-06-30 下午)

1. **1MHz 相干欠采样彻底验证成功**：
   - 物理输入 1MHz 时，利用 800kHz 欠采样（$p=1$），系统仅需 2 个窗口（`iter=2`）即收敛，提取幅度比高达 `|H|=1.0008`（误差 < 0.1%）。
2. **底层 Bug 已清扫**：
   - 修复了 `AD9833` `float` 转整数时的精度截断丢位问题，改用 `double` 高精度计算。
   - 兼容了外部独立 DDS 测试模式，解决了外部源固定频率与系统扫频脱节导致采集全线崩溃的问题。
3. **物理偏差实锤验证 + 校准闭环**：
   - 1MHz 观测到稳定的通道间相位差(早先 -0.96°,换回 PB1 后约 -0.67°),证明采集系统的高时间分辨率。
   - 偏斜不同上电有 ~0.3° 漂移;当前用硬编码 `CAL_SKEW_S=1.86ns` 修正,STAGE3 同源验证 1MHz/100kHz 校准后均 ~0°。需更高精度可切运行时 thru-cal。

> 备注:ADC CH2 一度从 PB1 误换到 PB0,读数被核心板 LED 支路分压成 ~17%;换回 PB1(SMA)即恢复。选 ADC 脚要挑板上没接东西的。
