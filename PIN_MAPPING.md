# 引脚映射与外设分配 (PIN_MAPPING)

本文档记录了目前工程的硬件引脚映射状态，主要用于后续 Stage 中验证功能是否对齐拓展板丝印。

## 关键引脚冲突 (Gotcha)

> [!WARNING]
> **ADC 引脚丝印反置问题**
>
> 拓展板丝印定义为：
> - `ADC1` = `PB1`
> - `ADC2` = `PC4`
>
> 但当前 STM32CubeMX 底层生成的代码 (`adc.c`) 配置恰好相反：
> - `ADC1_INP4` 绑定了 `PC4`
> - `ADC2_INP5` 绑定了 `PB1`
>
> **处理方案**：根据用户指示，代码保持原样不改，板上的丝印已经定死。在后续编写信号采集逻辑（特别是解析哪个通道的数据时）需要注意：软件读取到的 ADC1 数据物理上对应丝印的 ADC2 接口，ADC2 数据对应丝印的 ADC1 接口。

## 基础信号链路引脚

| 外设     | 引脚  | 状态       | 备注 |
| -------- | ----- | ---------- | ---- |
| **ADC1** | PC4   | *未验证*   | 对应丝印 ADC2 |
| **ADC2** | PB1   | *未验证*   | 对应丝印 ADC1 |
| **DAC1** | PA4   | *未验证*   | 对应丝印 DAC1 |
| **DAC2** | PA5   | *未验证*   | 对应丝印 DAC2 |

## 通信与时钟引脚

| 外设         | 引脚       | 状态      | 备注 |
| ------------ | ---------- | --------- | ---- |
| **UART1 TX** | PA9        | **已验证** | Stage -1 Loopback 回环成功 |
| **UART1 RX** | PA10       | **已验证** | Stage -1 Loopback 回环成功 |
| **TIM3 ETR** | PD2        | *未验证*   | 外部时钟输入引脚 |
| **AD9833**   | 原配置SPI  | *未验证*   | 用户确认配置无误，后续Stage验证 |
| **SI5351**   | 原配置I2C  | *未验证*   | 用户确认配置无误，后续Stage验证 |

## 验证说明
在进入涉及到具体外设驱动（例如 Stage 1/2）时，需要参考本表确保控制的引脚是正确的。所有 *未验证* 的外设需在相关 Stage 结束时使用 Python 脚本或逻辑分析仪进行闭环验收。
