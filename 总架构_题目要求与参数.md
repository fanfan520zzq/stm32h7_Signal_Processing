# 2025G 电路模型探究装置总架构

本文档面向题目要求整理当前工程总体方案、运行链路和关键参数。当前工程已经从早期 `DEBUG_STAGE` 调试主线，切到串口屏 HEX 命令驱动的集成主流程；原 STAGE 调试代码仍保留在 `main.c` 中用于分阶段验证。

## 1. 题目目标拆解

| 题目部分 | 目标 | 当前软件对应模块 |
|---|---|---|
| 基础(1) | 已知二阶电路模型 `H(s)=5/(1e-8*s^2+3e-4*s+1)` | `known_model.c` |
| 基础(2) | 输出正弦，频率可设，要求覆盖到 MHz 级 | `AD9833` 外部 DDS |
| 基础(3) | 给定 1kHz，按已知 `H(s)` 设输入幅度，使已知电路输出约 2Vpp | `KnownModel_Update()` |
| 基础(4) | 100Hz~3kHz，输出目标 1~2V，0.1V 步进 | 串口屏 `SET_FREQ/SET_AMP` + 已知模型补偿 |
| 发挥(1) | 未知 RLC 网络自主学习，2 分钟内判断滤波类型 | `sweep_engine.c` + `sweep_grid.c` + `classify.c` |
| 发挥(2) | 学习后对 1k~50kHz 输入信号复现未知网络输出 | `recon_analyzer.c` + `recon_synth.c` + `recon_dds.c` + `recon_pll.c` |

## 2. 当前运行模式

当前 `main()` 初始化外设后进入串口屏命令状态机，不再自动执行 `DEBUG_STAGE=15` 的全链路调试函数。

主循环逻辑：

1. `UART_Poll()` 解析串口屏 6 字节 HEX 命令。
2. 根据命令进入以下状态：
   - `CMD_RESET`: 停止输出。
   - `CMD_LEARN`: 扫频学习未知网络，分类并显示滤波器类型。
   - `CMD_RECON`: 基于当前输入进行重建输出，并持续 PLL 锁相。
   - `CMD_SET_FREQ`: 基础题频率设置，调用已知模型补偿。
   - `CMD_SET_AMP`: 基础题幅度设置，调用已知模型补偿。
3. 当 `current_state == CMD_RECON` 时，循环调用 `Recon_RunPllTick()`。

## 3. 硬件与引脚

| 功能 | 器件/外设 | 引脚/通道 | 说明 |
|---|---|---|---|
| 未知/输入采样 CH1 | ADC1 | PC4 / ADC_CHANNEL_4 | 重建输入、扫频参考 |
| DUT 输出采样 CH2 | ADC2 | PB1 / ADC_CHANNEL_5 | 扫频测量输出 |
| 重建输出 | DAC1 CH1 | PA4 | DMA + TIM6 触发 |
| 备用 DAC | DAC1 CH2 | PA5 | 工程保留 |
| 外部 DDS | AD9833 | SPI1 + PG10 FSYNC | 扫频/基础信号源 |
| AD9833 幅度控制 | 数字电位器 | PB5 CS | `AD9833_SetAmplitude()` |
| 串口屏 | USART1 | PB14/PB15 | 6 字节 HEX 协议 |

## 4. 串口屏协议

串口屏命令帧长度固定 6 字节：

```text
AD <cmd/payload0> <payload1> <payload2> <payload3> DA
```

| 命令 | 字节格式 | 当前行为 |
|---|---|---|
| 复位 | `AD FF FF FF FF DA` | 停止 AD9833/DAC 重建输出，回到空闲 |
| 学习 | `AD 45 FE FE FE DA` | 扫频未知网络，分类并显示到 `t0` |
| 重建 | `AD 67 EE EE EE DA` | 从当前 PC4 输入分析谐波并启动 PA4 重建 |
| 设频 | `AD 22 f0 f1 f2 DA` | `freq = f0 + f1<<8 + f2<<16` |
| 设幅 | `AD 34 amp_x10 xx xx DA` | `target_vpp = amp_x10 / 10.0` |

## 5. 基础题已知模型链路

文件：

- `Core/Inc/known_model.h`
- `Core/Src/known_model.c`

已知模型：

```text
H(s) = 5 / (1e-8*s^2 + 3e-4*s + 1)
s = j * 2*pi*f
```

实现流程：

1. 串口屏发送目标频率 `g_target_freq` 和目标输出幅度 `g_target_vpp`。
2. 计算 `|H(jw)|`。
3. 计算所需输入幅度：

```text
required_vin_vpp = target_vpp / |H(jw)|
```

4. 转换成 AD9833 幅度码：

```text
amp_val = required_vin_vpp * CALIB_K
CALIB_K = 36.5
amp_val clamp 到 0..255
```

5. 调用：

```c
AD9833_SetFixedOutput(freq_hz, WAVE_SINE);
AD9833_SetAmplitude((uint8_t)amp_val);
```

当前状态：逻辑已写入，基础部分仍需板上完整验收。

## 6. 扫频学习链路

目标：学习未知 RLC 网络的频率响应表 `H(f)`，并判断滤波器类型。

数据流：

```text
AD9833 输出扫频
  -> DUT 输入
  -> ADC1 采参考输入
  -> ADC2 采 DUT 输出
  -> Goertzel 单频点复数比值
  -> H_table[f, |H|, phase]
  -> 分类 LP/HP/BP/BS/UNKNOWN
  -> 串口屏显示
```

核心文件：

| 文件 | 作用 |
|---|---|
| `sweep_engine.c` | 单点测量、相干/欠采样参数计算、写入 `g_Htable` |
| `sweep_grid.c` | 粗扫、1k~50k 精扫、局部补点 |
| `goertzel.c` | 单 bin Goertzel，计算两路复数比值 |
| `calib.c` | 通道偏斜/增益校准 |
| `classify.c` | 按幅频形状判断滤波器类型 |

### 6.1 扫频范围与网格

当前集成命令 `CMD_LEARN` 调用：

```c
sweep_engine_run(100.0f, 1000000.0f);
```

`sweep_grid_execute()` 内部分三阶段：

| 阶段 | 频率范围/策略 | 参数 |
|---|---|---|
| 粗扫 | `100Hz..1MHz` 对数扫频 | 普通区 `8 pts/dec`，RLC 关键区 `800Hz..80kHz` 为 `16 pts/dec` |
| 精扫 | `1kHz..50kHz` | 步进 `200Hz` |
| 局部补点 | 峰/谷、-3dB 附近 | `±600Hz`，步进 `50Hz` |

H 表容量：

```c
#define H_TABLE_MAX 1024
```

### 6.2 单点采样参数

扫频单点使用相干采样/欠采样：

```text
f_actual = (p + 1/N) * Fs
```

关键参数：

| 参数 | 当前值 | 说明 |
|---|---:|---|
| `TIM_KER_CLK_HZ` | `240000000 Hz` | TIM4 内核时钟 |
| `ADC_KER_CLK_HZ` | `75000000 Hz` | ADC 内核时钟 |
| `FS_MAX_HZ` | `1200000 Hz` | 已验证 ADC 均匀采样上限 |
| `N` | `floor(FS_MAX/target_f)` 后 clamp 到 `4..32` | 每混叠周期采样点 |
| `M_DEFAULT` | `16` | 采样窗口周期数 |
| `M_LOW_FREQ` | `8` | 低频窗口周期数 |
| `L_MAX` | `512` | 单点采样最大长度 |
| 单点缓冲 | `ch2[L_MAX]`, `ch3[L_MAX]`, `float` 转换缓冲 | `static` 避免栈溢出 |
| 稳定阈值 | `SETTLE_TH = 0.005` | 幅值连续变化小于 0.5% 判稳 |
| 最大等待 | `max_timeout = 20` | 单点最多迭代 20 次 |

单点测量输出字段：

```c
HPoint {
    float f_actual;
    float H_mag;
    float H_phase;
    uint8_t resolution;
    uint8_t settled;
}
```

### 6.3 分类策略

`sweep_analyze()` 读取 `g_Htable`：

- 计算最大/最小 dB。
- 对左右边缘点求平均，判断低频端/高频端是否被抑制。
- 判断中间峰值或陷波。
- 找 -3dB 截止点，计算几何中心、带宽和 Q。

类型输出：

```text
FILT_LP
FILT_HP
FILT_BP
FILT_BS
FILT_ALLPASS
FILT_UNKNOWN
```

当前串口屏显示：

```text
t0.txt="LP" / "HP" / "BP" / "BS" / "UNKNOWN"
```

## 7. 重建输出链路

目标：未知网络学习完成后，对外部输入的正弦/方波等信号，重建其经过未知网络后的输出。

数据流：

```text
PC4 输入采样
  -> f0 测量
  -> 谐波 DFT 分析
  -> 对每个谐波查 H(f)
  -> 幅度乘 |H|，相位加 ∠H
  -> 生成 1024 点 DAC LUT
  -> PA4 DAC DMA 输出
  -> PLL 根据输入基波相位微调 DDS 相位/频率
```

核心文件：

| 文件 | 作用 |
|---|---|
| `recon_analyzer.c` | 输入频率、Vpp、谐波幅相分析 |
| `recon_hlookup.c` | H 表插值查表 |
| `recon_synth.c` | 按谐波合成 DAC LUT |
| `recon_dds.c` | DAC DDS 相位累加、DMA 输出 |
| `recon_pll.c` | 输入相位锁定 |

### 7.1 重建采样参数

ADC 原始采样缓冲：

```c
#define LEN 2048
uint16_t CH1_Buffer[LEN];
uint16_t CH2_Buffer[LEN];
```

重建分析采样：

| 场景 | TIM4 参数 | 采样率 | 说明 |
|---|---|---:|---|
| 快速默认 | `psc=0, arr=199` | `1.2MHz` | 覆盖中高频和较高谐波 |
| 低频二次采样 | `psc=0, arr=1199` | `200kHz` | 当 `f0 < 3kHz` 时重采，提高 1k 附近周期数 |

采样率计算：

```text
Fs = TIM_KER_CLK_HZ / ((psc + 1) * (arr + 1))
```

### 7.2 谐波分析限制

当前保守上限方案：

```c
#define RECON_MAX_HARMONICS 64
```

谐波保留条件：

```c
hf = f0 * k
hf <= 200050 Hz
hf < fs_hz * 0.45
k <= 64
```

幅度门限：

```c
min_keep = max(fund_vpp * 0.01, 0.005Vpp)
```

非基波小于该门限时不参与重建。

整数周期 DFT：

```text
samples_per_period = fs_hz / f0
integer_periods = floor(len / samples_per_period)
dft_len = integer_periods * samples_per_period
```

这样减少非整数周期导致的频谱泄漏和相位跳动。

实际含义：

| 输入基波 | 快速采样理论 | 当前有效上限 |
|---:|---|---|
| `1kHz` | 最高 64 次，即 64kHz | 低频采样路径 `200kHz * 0.45 = 90kHz`，64 次可覆盖 |
| `10kHz` | 最高 20 次到 200kHz | 受 200kHz 上限限制 |
| `20kHz` | 最高 10 次到 200kHz | 受 200kHz 上限限制 |
| `50kHz` | 最高 4 次到 200kHz | 受 200kHz 上限限制 |

### 7.3 LUT 与 DAC 输出

关键参数：

```c
#define RECON_TABLE_LEN 1024
#define RECON_DAC_FS_HZ 1000000.0f
```

合成参数：

```c
recon_synth_build_lut(analysis, lut, 1024, 1.65f, 3.2f, &used)
```

| 参数 | 值 | 说明 |
|---|---:|---|
| LUT 长度 | `1024` | 单周期波表 |
| DAC 采样率 | `1MHz` | TIM6 驱动 |
| DAC 中点 | `1.65V` | 输出居中 |
| 最大输出 Vpp | `3.2Vpp` | 超过则整体缩放，避免削顶 |
| DAC 分辨率 | `12 bit` | 0..4095 |

合成公式：

```text
每个输入谐波 x_k:
  rel_phase = x_phase - k * fundamental_phase
  output_amp = 0.5 * x_vpp * |H(k*f0)|
  output_phase = rel_phase + ∠H(k*f0)
```

### 7.4 PLL 锁相

当前参数：

```text
Kp = 0.6
Ki = 0.005
```

入口：

- 初次 `CMD_RECON`
- 自动输入变化后重建
- PLL relock

当前都统一使用以上参数。

时间基准：

```c
Recon_EnableDwt();
g_adc_start_dwt = DWT->CYCCNT;
dt = (now - last_tick) / 480000000.0
```

当前集成主流程已在系统初始化和进入 `CMD_RECON` 前开启 DWT。

自动重建触发：

```c
#define RECON_AUTO_REBUILD_CONFIRM 3
```

连续 3 帧满足变化条件才自动重建：

| 条件 | 阈值 |
|---|---:|
| 频率变化 | `max(8Hz, 0.2%)` |
| 幅度变化 | `12%` |
| 谐波结构变化 | 2..7 次谐波相对比例差值 `>0.06` |

## 8. AD9833 与输出关闭

AD9833 正常定频输出：

```c
AD9833_SetFixedOutput(freq, WAVE_SINE);
AD9833_SetAmplitude(amp);
```

彻底关闭函数：

```c
AD9833_OutputDisable();
```

内部操作：

```c
AD9833_AmpSet(0);
AD9833_Write16(0x21C0); // B28 | RESET | SLEEP1 | SLEEP12
```

注意：当前 `CMD_LEARN` 路径里扫频后使用的是：

```c
AD9833_SetAmplitude(0);
AD9833_SetFixedOutput(0, WAVE_SINE);
```

如果严格要求“扫频后完全关闭 AD9833”，建议改回调用 `AD9833_OutputDisable()`。

## 9. 关键数组与内存

| 数组/缓冲 | 大小 | 位置/用途 |
|---|---:|---|
| `CH1_Buffer[LEN]` | `2048 * uint16_t` | ADC1 DMA |
| `CH2_Buffer[LEN]` | `2048 * uint16_t` | ADC2 DMA |
| `g_dma_buf[L_MAX]` | `512 * uint32_t` | ADC 同步采样封装 |
| `g_Htable[H_TABLE_MAX]` | `1024 * HPoint` | 扫频 H 表 |
| `recon_lut[RECON_TABLE_LEN]` | `1024 * uint16_t` | 当前重建波表 |
| `s_recon_lut[RECON_TABLE_LEN]` | `1024 * uint16_t` | DDS 内部重建表 |
| `Buffer1[1024]` | `1024 * uint16_t` | DAC CH1 DMA |
| `Buffer2[1024]` | `1024 * uint16_t` | DAC CH2 DMA |
| `temp[RECON_TABLE_LEN]` | `1024 * float` | LUT 合成临时浮点缓冲 |
| `ReconAnalysis.harmonic[64]` | 64 个谐波结构体 | 输入谐波分析结果 |
| `LCD wave_buffer` | `480 * uint8_t` | Nextion 波形显示 |

构建观测：启用 64 谐波后 DTCM 占用约 `46KB / 128KB`，RAM 占用约 `77KB / 512KB`，仍有余量。

## 10. 主要文件索引

| 文件 | 职责 |
|---|---|
| `Core/Src/main.c` | 串口屏状态机、基础/学习/重建入口 |
| `Core/Task/UARTTX.c` | 6 字节 HEX 命令解析 |
| `Core/Src/known_model.c` | 基础题已知模型幅度补偿 |
| `Core/Src/sweep_engine.c` | 单点扫频测量 |
| `Core/Src/sweep_grid.c` | 扫频网格规划 |
| `Core/Src/classify.c` | 滤波器类型与截止频率分析 |
| `Core/Src/recon_analyzer.c` | 输入谐波分析 |
| `Core/Src/recon_synth.c` | 输出 LUT 生成 |
| `Core/Src/recon_dds.c` | DAC DDS 输出 |
| `Core/Src/recon_pll.c` | PLL 锁相控制 |
| `Core/AD9833/ad9833_hal.c` | AD9833 频率/幅度/关闭 |
| `Core/Task/ADCTask.c` | 双 ADC 同步 DMA 采样 |
| `Core/Task/DDS.c` | DAC DMA 半缓冲填充 |
| `Core/Task/LCD.c` | Nextion 指令与波形发送 |

## 11. 当前测试状态

已验证/基本可用：

- 双 ADC 同步采样。
- AD9833 扫频输出。
- Goertzel 单点 H(f) 测量。
- 重建链路的基本波形输出。
- 1k 低频重建通过低频采样路径改善。
- DWT 开启后 PLL 明显稳定。

已写但仍需完整测试：

- 基础题已知模型 `KnownModel_Update()` 全流程。
- 串口屏 HEX 命令实际交互。
- `CMD_LEARN` 学习路径完整板测。
- 自动输入变化重建。
- 多种未知网络类型 LP/HP/BP/BS 泛化。
- 64 谐波 / 200kHz 上限对方波重建质量的实际收益。

主要风险：

- 当前 `CMD_LEARN` 扫频后未调用 `AD9833_OutputDisable()`，若需要严格静默应修正。
- 串口屏协议替换后，旧 ASCII `R/D` 调试命令不可用。
- `Recon_PrintDebugChain()` 当前未被调用，编译有 unused warning。
- 高次谐波虽然分析到 200kHz，但 DAC 为 1MHz 输出，100kHz 以上主要改善边沿，不应期待完全理想波形。
- 低频路径使用 200kHz 采样，上限受 `fs*0.45=90kHz` 限制；如需 1k 方波更高次谐波，应增加“双采样”策略。

## 12. 建议验收顺序

1. 基础题：串口屏设频/设幅，测已知模型输出 Vpp，校准 `CALIB_K`。
2. 学习：接已知低通/带通/带阻，执行 `CMD_LEARN`，确认 `t0` 类型显示正确。
3. 重建：执行 `CMD_RECON`，输入 1k/10k 正弦，确认同频稳定、幅度接近。
4. 波形：输入方波，观察 64 谐波上限后的边沿改善。
5. 自适应：不重新按重建按钮，切换输入频率/幅度/波形，确认自动重建触发并稳定。
6. 长测：连续运行，检查 AD9833 关闭、DAC 输出、PLL relock 是否符合预期。
