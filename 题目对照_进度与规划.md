# 面向题目的进度与规划 — 2025G 电路模型探究装置

把当前进度对着题目要求摆一遍,看哪些到位、哪些还缺。日期:2026-06-30。

---

## 一、题目拆解(探究装置要会的事)

| 条目 | 要求 | 本质功能 |
|------|------|----------|
| 基本(1) | 搭 `H(s)=5/(1e-8·s²+3e-4·s+1)` 的已知电路(2阶低通,f0≈1592Hz,DC增益5,Q≈0.33) | 硬件(单独板) |
| 基本(2) | 探究装置出正弦,频率可设(100Hz步进)、**≥1MHz**、误差<5%、Vpp≥3V | **信号源** |
| 基本(3) | 出1kHz,**按H(s)算输入幅度**使已知电路输出=2Vpp,误差<5% | **幅度闭环**(算 Vin=Vout/\|H\|) |
| 基本(4) | 100Hz~3kHz,输出设定值1~2V(0.1V步)同上 | **幅度闭环**(全频段) |
| 发挥(1) | 连未知RLC输入输出,2分钟内**自主学习建模,显示滤波类型** | **扫频测H(f)+判类型** |
| 发挥(2) | 学完当滤波器用,输入1k~50kHz(200Hz步,正弦/方波/其他)**复现未知电路输出**(Vpp误差<10%,无失真,相位无要求) | **H(s)建模+实时复现** |

**关键约束**:启动5s内出信号;发挥(1)两分钟内完成、不可人工干预;发挥(2)输入阻抗≥100kΩ;一键启动。

**未知电路关键参数**:RLC各一个 → **必是2阶**,类型 ∈ {低通/高通/带通/带阻}。
`f0=1/(2π√LC)`,L(1~10mH)/C(10~100nF) → **f0 ∈ 5~50kHz**;`Q=(1/R)√(L/C)≈316/R ≈ 0.03~0.3`(**低Q,峰谷平缓**)。

---

## 二、现在做到哪了(对照)

| 功能 | 状态 | 说明 |
|------|------|------|
| 信号源(AD9833) | ✅ 大部分 | 频率到1MHz验证、准;Vpp约2.6V(题目要≥3V,可能要调幅度链) |
| 双路同步采集 | ✅ | 独立双ADC+共享TIM4触发,12bit |
| H(f) 测量(相干/欠采样) | ✅ | 测到1MHz,\|H\|和∠H,通道偏斜已校准(~0°) |
| 幅度闭环(基本3/4) | ❌ | AD9833_AmpSet有,但"按H算Vin、Vpp定标"逻辑没写 |
| 自适应扫频网格 | ❌ | sweep_grid只有对数粗扫,细化是桩 |
| 类型判别(发挥1) | ❌ | 还没做 |
| H(s)拟合 + 复现(发挥2) | ❌ | 还没做 |
| 人机(按键/显示/一键启动) | ❌ | 现在靠改DEBUG_STAGE重编译;要接Nextion+按键 |

**一句话**:测量内核(发挥1的地基)已经扎实,上层的"判类型/拟合/复现/人机"都还没动。

---

## 三、按题目分线规划

### A. 基本要求线(信号源 + 幅度闭环)
1. **Vpp 定标**:测 AD9833 数字电位器档位 vs 实际输出Vpp,建表/拟合 → 能"设定Vpp"。
2. **幅度闭环**:基本(3)(4)给定 Vout 和 f,算 `Vin = Vout / |H(s)(f)|`(H已知,公式直接算),设到信号源。
3. 频率步进、≥3Vpp、≥1MHz 这些信号源指标核对达标。

### B. 发挥(1)线(学习建模 → 判类型)★当前主攻
1. **自适应扫频**:粗扫(对数)找大致形状 → 在 f0(峰/谷)附近加密。范围覆盖 ~500Hz~500kHz(把5~50kHz的f0和滚降都包住)。
2. **类型判别**:看 |H| 形状——低频/峰谷/高频三段比大小:
   - LP:低平→高掉;HP:低掉→高平;BP:中间峰;BS:中间谷。
   - 难点:低Q峰谷浅,要测得准 + 网格够密别误判。
3. **显示**:Nextion 显类型(+ f0、增益)。2分钟够用。

### C. 发挥(2)线(复现)
1. 在发挥(1)基础上拟合 2阶 `H(s)`(类型+f0+Q+增益,4参数,片上网格搜索)。
2. 复现两条路(待定):
   - **实时数字IIR**:ADC→IIR(双线性变换的H)→DAC,通用、对任意输入。
   - **DFT谐波合成**:测输入各次谐波→逐谐波套H→重建,需锁定基波。
3. 输入阻抗≥100kΩ的前端要满足。

---

## 四、关键技术点备忘

- 未知电路 f0=5~50kHz 在**过采样区**,核心测量不依赖欠采样;欠采样用于看高频滚降/谐波(发挥2复现要H到几百kHz)和满足1MHz信号源指标。
- 低Q(0.03~0.3):2阶但很"温和",LP/HP无谐振峰,BP/BS峰谷浅 → 判类型靠整体走向+测量精度。
- 相位:发挥(1)判类型其实主要用|H|;发挥(2)复现要各次谐波的相对相位(∠H),通道偏斜校准已就位。

---

## 五、建议下一步顺序

1. **发挥(1)闭环先打通**(主攻):自适应网格 + 类型判别,先在串口出"类型+f0",再上屏。
2. 并行补**基本要求**的幅度闭环(Vpp定标 + Vin=Vout/|H|)。
3. 最后啃**发挥(2)复现**。

(测量内核细节见 `扫频模块_进度报告与待办.md`)
